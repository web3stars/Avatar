
import { expect } from "chai";
import { BigNumber } from "ethers";
import { ethers } from "hardhat";
export const cryptoPetsTest = async function () {

    // const Greeter = await ethers.getContractFactory("Greeter");
    // const greeter = await Greeter.deploy("Hello, world!");
    // await greeter.deployed();
  
    // expect(await greeter.greet()).to.equal("Hello, world!");
  
    // const setGreetingTx = await greeter.setGreeting("Hola, mundo!");
  
    // // wait until the transaction is mined
    // await setGreetingTx.wait();
  
    // expect(await greeter.greet()).to.equal("Hola, mundo!");
  
    // const CryptoPets = await ethers.getContractFactory("CryptoPets");
  
    // const cryptoPets = await CryptoPets.attach("0x01c1DeF3b91672704716159C9041Aeca392DdFfb");
    // console.log(await cryptoPets.generateSVG(4));
    // console.log(Buffer.from(await cryptoPets.generateSVG(4) ,"base64").toString());
    // console.log(await cryptoPets.pets(4));
  };
  
 
  
  